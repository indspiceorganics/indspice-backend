# Created by https://www.toptal.com/developers/gitignore/api/journal # SQLite journal file
media/ # Optional: Ignore if media files are user uploads managed separately
staticfiles/ # Optional: Ignore if collectstatic is run on the server

# Virtual Environments
venv/
env/
ENV/
virtualpython,django,visualstudiocode,windows,macos,linux,dotenv
# Edit at https://www.toptal.com/developers/gitignore?templates=python,django,visualstudiocode,windows,macos,linux,dotenv

env/
*/.venv/
.venv/ # Common name

# Environment variables
.env
.env.*
!.env.example # Do not ignore example env file

# PyInstaller
# Usually ignored
*.spec

# Installer### Django ###
*.log
*.pot
*.pyc
__pycache__/
local_settings.py
db.sqlite3
db.sqlite3-journal
media/ # If you store user uploads locally - better to logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests. use cloud storage in prod
staticfiles/ # Usually rebuilt during deployment
/static/ # Generally shouldn't commit built static assets directly

# Scrapy stuff:
.scrapy

### dotenv ###
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Jupyterlocal

# Generated by Cargo
# will have compiled files and more
target/

### Linux ###
*~

# KDE directory preferences
.directory

# Linux trash folder which might appear on external disk
.Trash-*

# Coredump files
core.*

### macOS ###
# General
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon


# Thumbnails
._*

# Files that might Notebook
.ipynb_checkpoints

# pyenv
.python-version

# IDE / Editor specific files
.idea/
.vscode/
*.swp
*~
.#*

# OS generated files
.DS_Store
Thumbs.db
desktop.ini

# Optional: Sensitive config files (should use env vars instead)
# config appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.yaml
# settings_local.py