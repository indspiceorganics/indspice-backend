# backend/requirements.txt
fastapi==0.111.0
uvicorn[standard]==0.29.0 # ASGI server + extras like websockets, http tools
pydantic[email]==2.7.1   # Data validation, includes email validation
python-dotenv==1.0.1   # For loading .env file locally
gunicorn==22.0.0         # Production process manager (runs uvicorn workers)

# Add other dependencies if needed later (e.g., for database: SQLAlchemy, psycopg2-binary)